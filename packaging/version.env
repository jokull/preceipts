# Version of the bundle, read by package.sh. Kept out of Cargo.toml's reach on
# purpose: CFBundleVersion has to climb on every notarized upload even when the
# crate version does not move.
MARKETING_VERSION=0.1.0
BUILD_NUMBER=1
