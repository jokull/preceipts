{
  "name": "preceipts",
  "version": "0.1.0",
  "description": "Local CI, proven by trees, merged by humans. Tree-keyed check receipts stored in git notes.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "preceipts": "./src/cli.ts"
  },
  "exports": {
    ".": "./src/lib/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/cli.ts --outfile dist/preceipts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.0"
  }
}
